# ==============================================================================
# Sentinel Flow Panel Config (Anonymized)
# ==============================================================================
# Public-safe version – no infra disclosure
# Wagner N Nascimento ©
# ==============================================================================

cellIdPreamble: "cell-"
gradientMode: "hue"

# ------------------------------------------------------------------------------
# Appearance & Highlighting
# ------------------------------------------------------------------------------
tagConfig:
  legend:
    - "Status:"
    - "OK"
    - "WARN"
    - "CRIT"
  color: "yellow"
  highlightRgbFactor: 5
  lowlightAlphaFactor: 1

# ------------------------------------------------------------------------------
# Cells
# ------------------------------------------------------------------------------

cells:

  # ---------------------------------------------------------------------------
  # ENABLED NODES
  # ---------------------------------------------------------------------------

  node_01_status:
    dataRef: node_01_status
    label: { separator: replace }
    labelColor:
      thresholds:
        - { color: "semi-dark-green", level: 0 }
        - { color: "orange", level: 1 }
        - { color: "red", level: 2 }
    tags: ["enabled", "active"]

  node_02_status:
    dataRef: node_02_status
    label: { separator: replace }
    labelColor:
      thresholds:
        - { color: "semi-dark-green", level: 0 }
        - { color: "orange", level: 1 }
        - { color: "red", level: 2 }
    tags: ["enabled", "active"]

  node_03_status:
    dataRef: node_03_status
    label: { separator: replace }
    labelColor:
      thresholds:
        - { color: "semi-dark-green", level: 0 }
        - { color: "orange", level: 1 }
        - { color: "red", level: 2 }
    tags: ["enabled", "active"]

  node_04_status:
    dataRef: node_04_status
    label: { separator: replace }
    labelColor:
      thresholds:
        - { color: "semi-dark-green", level: 0 }
        - { color: "orange", level: 1 }
        - { color: "red", level: 2 }
    tags: ["enabled", "active"]

  node_05_status:
    dataRef: node_05_status
    label: { separator: replace }
    labelColor:
      thresholds:
        - { color: "semi-dark-green", level: 0 }
        - { color: "orange", level: 1 }
        - { color: "red", level: 2 }
    tags: ["enabled", "active"]

  node_06_status:
    dataRef: node_06_status
    label: { separator: replace }
    labelColor:
      thresholds:
        - { color: "semi-dark-green", level: 0 }
        - { color: "orange", level: 1 }
        - { color: "red", level: 2 }
    tags: ["enabled", "active"]

  node_07_status:
    dataRef: node_07_status
    label: { separator: replace }
    labelColor:
      thresholds:
        - { color: "semi-dark-green", level: 0 }
        - { color: "orange", level: 1 }
        - { color: "red", level: 2 }
    tags: ["enabled", "active"]

  node_08_status:
    dataRef: node_08_status
    label: { separator: replace }
    labelColor:
      thresholds:
        - { color: "semi-dark-green", level: 0 }
        - { color: "orange", level: 1 }
        - { color: "red", level: 2 }
    tags: ["enabled", "active"]

  # ---------------------------------------------------------------------------
  # DISABLED / INACTIVE NODES
  # ---------------------------------------------------------------------------

  node_09_status:
    dataRef: node_09_status
    label: { separator: replace }
    labelColor:
      thresholds:
        - { color: "gray", level: 0 }
    tags: ["disabled"]

